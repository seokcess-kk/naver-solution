# 네이버 플레이스 모니터링 시스템 - 실제 데이터 테스트 가이드

## 서버 상태

✅ **백엔드**: http://localhost:8000
✅ **프론트엔드**: http://localhost:3000
✅ **데이터베이스**: PostgreSQL 연결됨

---

## 📋 테스트 체크리스트

### 1️⃣ 인증 시스템 테스트

#### 회원가입 테스트
1. 브라우저에서 http://localhost:3000/register 접속
2. 다음 정보로 계정 생성:
   - 이름: `테스트 사용자`
   - 이메일: `test@example.com`
   - 비밀번호: `Test1234!`
3. **확인 사항**:
   - [ ] 회원가입이 성공적으로 완료됨
   - [ ] 자동으로 로그인되어 대시보드로 리다이렉트됨
   - [ ] 액세스 토큰과 리프레시 토큰이 localStorage에 저장됨

#### 로그인 테스트
1. 로그아웃 후 http://localhost:3000/login 접속
2. 위에서 생성한 계정으로 로그인
3. **확인 사항**:
   - [ ] 로그인 성공
   - [ ] 대시보드로 리다이렉트됨
   - [ ] 사용자 이름이 헤더에 표시됨

---

### 2️⃣ Place 생성 및 관리 테스트

#### Place 생성
1. 대시보드에서 "Places" 메뉴 클릭
2. "새 Place 추가" 버튼 클릭
3. 실제 네이버 플레이스 정보 입력 (예시):
   - **이름**: `스타벅스 강남역점`
   - **Naver Place ID**: `13491210` (실제 네이버 플레이스 ID)
   - **주소**: `서울 강남구 강남대로 396`
   - **카테고리**: `카페`
4. **확인 사항**:
   - [ ] Place가 성공적으로 생성됨
   - [ ] Place 목록에 새로 생성된 Place가 표시됨
   - [ ] Place 상세 페이지로 이동 가능

#### Place 목록 기능 테스트
1. Place 목록 페이지에서 검색 기능 테스트
   - 검색어: `스타벅스`
   - [ ] 검색 결과가 정확하게 필터링됨
2. 정렬 기능 테스트
   - 이름순/생성일순 정렬
   - 오름차순/내림차순 변경
   - [ ] 정렬이 올바르게 동작함
3. 필터링 테스트
   - 활성/비활성 필터
   - [ ] 필터가 올바르게 동작함

---

### 3️⃣ 키워드 및 랭킹 스크래핑 테스트 ⭐ (핵심 기능)

#### 키워드 추가
1. 생성한 Place의 상세 페이지로 이동
2. "키워드 추가" 버튼 클릭
3. 다음 정보 입력:
   - **키워드**: `강남역 카페`
   - **지역**: `서울 강남구`
4. 추가 키워드 생성 (선택):
   - **키워드**: `강남 스타벅스`
   - **지역**: `서울 강남구`
5. **확인 사항**:
   - [ ] 키워드가 성공적으로 추가됨
   - [ ] 키워드 목록에 표시됨

#### 랭킹 스크래핑 테스트
1. 추가한 키워드의 "순위 확인" 또는 "스크래핑" 버튼 클릭
2. **⏱️ 대기 시간**: Puppeteer가 네이버 페이지를 로드하고 데이터를 추출하는 데 10-30초 소요
3. **확인 사항**:
   - [ ] 스크래핑이 시작됨 (로딩 인디케이터 표시)
   - [ ] 스크래핑이 성공적으로 완료됨
   - [ ] 순위 데이터가 표시됨 (1-100위 중 순위 또는 "순위권 밖")
   - [ ] 백엔드 콘솔에서 스크래핑 로그 확인 가능

#### 랭킹 히스토리 및 차트 테스트
1. 키워드의 "순위 보기" 또는 "랭킹 히스토리" 클릭
2. **확인 사항**:
   - [ ] 랭킹 히스토리가 차트로 표시됨
   - [ ] 날짜 필터링이 동작함 (Calendar UI)
   - [ ] 차트 확대/축소 기능이 동작함 (Brush)
   - [ ] CSV 내보내기가 가능함

#### 스크래핑 실패 시 디버깅
만약 스크래핑이 실패하면:
1. 백엔드 콘솔에서 에러 로그 확인
2. 네이버 플레이스 ID가 올바른지 확인
3. 키워드와 지역이 실제 네이버 검색에서 결과를 반환하는지 확인
4. Puppeteer 타임아웃 설정 확인 (`.env`의 `PUPPETEER_TIMEOUT`)

---

### 4️⃣ 리뷰 스크래핑 및 감정 분석 테스트

#### 리뷰 스크래핑
1. Place 상세 페이지에서 "리뷰" 탭 클릭
2. "리뷰 스크래핑" 버튼 클릭
3. **⏱️ 대기 시간**: 리뷰 페이지 로드 및 파싱에 15-45초 소요
4. **확인 사항**:
   - [ ] 리뷰 목록이 성공적으로 로드됨
   - [ ] 각 리뷰에 감정 분석 결과가 표시됨 (긍정/부정/중립)
   - [ ] 리뷰 카드에 작성자, 평점, 날짜, 내용이 표시됨

#### 리뷰 필터링 및 분석
1. 감정별 필터링 테스트
   - 긍정/부정/중립 필터 클릭
   - [ ] 해당 감정의 리뷰만 표시됨
2. "리뷰 분석" 페이지로 이동
3. **확인 사항**:
   - [ ] 감정 비율 Pie Chart 표시
   - [ ] 시간대별 리뷰 수 변화 Line Chart 표시
   - [ ] 평균 평점 변화 Chart 표시
   - [ ] 감정 분포 변화 Chart 표시

---

### 5️⃣ 경쟁사 추적 및 비교 테스트

#### 경쟁사 추가
1. Place 상세 페이지에서 "경쟁사" 탭 클릭
2. "경쟁사 추가" 버튼 클릭
3. 경쟁 카페의 실제 네이버 플레이스 정보 입력:
   - **이름**: `투썸플레이스 강남역점`
   - **Naver Place ID**: `1234567890` (실제 ID로 교체)
   - **지역**: `서울 강남구`
4. **확인 사항**:
   - [ ] 경쟁사가 성공적으로 추가됨
   - [ ] 경쟁사 목록에 표시됨

#### 경쟁사 스냅샷 기록
1. 추가한 경쟁사의 "스냅샷 기록" 버튼 클릭
2. **⏱️ 대기 시간**: 경쟁사 데이터 수집에 10-20초 소요
3. **확인 사항**:
   - [ ] 스냅샷이 성공적으로 기록됨
   - [ ] 최신 통계가 업데이트됨 (순위, 평점, 리뷰 수)

#### 경쟁사 비교 차트
1. 경쟁사의 "상세 보기" 클릭
2. **확인 사항**:
   - [ ] 순위 추이 차트 표시
   - [ ] 평균 평점 추이 차트 표시
   - [ ] 블로그/방문자 리뷰 수 추이 차트 표시
   - [ ] 날짜 필터링이 동작함

---

### 6️⃣ 알림 시스템 테스트

#### 알림 설정 추가
1. "설정" → "알림 설정" 메뉴로 이동
2. "새 알림 추가" 버튼 클릭
3. 다음 정보로 알림 설정:
   - **Place**: 생성한 Place 선택
   - **알림 유형**: `ranking_change` (순위 변동)
   - **채널**: `email` 또는 `slack`
   - **조건**: `{"threshold": 5}` (5순위 이상 변동 시 알림)
4. **확인 사항**:
   - [ ] 알림 설정이 성공적으로 생성됨
   - [ ] 알림 목록에 표시됨
   - [ ] 활성/비활성 토글이 동작함

#### 알림 테스트
1. 랭킹 스크래핑을 여러 번 실행하여 순위 변동 발생시키기
2. **확인 사항**:
   - [ ] 조건에 맞는 경우 알림이 발송됨
   - [ ] 알림 로그에 발송 기록이 남음

---

### 7️⃣ 사용자 프로필 관리 테스트

#### 프로필 수정
1. 우측 상단 사용자 메뉴 → "프로필" 클릭
2. 이름 수정 테스트
   - 새 이름: `테스트 사용자 수정`
   - [ ] 프로필 정보가 성공적으로 업데이트됨
   - [ ] 헤더의 사용자 이름이 즉시 변경됨

#### 비밀번호 변경
1. 프로필 페이지에서 비밀번호 변경 폼 작성
   - 현재 비밀번호: `Test1234!`
   - 새 비밀번호: `NewPassword1234!`
   - 비밀번호 확인: `NewPassword1234!`
2. **확인 사항**:
   - [ ] 비밀번호가 성공적으로 변경됨
   - [ ] 로그아웃 후 새 비밀번호로 로그인 가능

---

### 8️⃣ 대시보드 통합 테스트

#### 대시보드 데이터 확인
1. 대시보드 페이지로 이동
2. **확인 사항**:
   - [ ] Places 요약 카드가 표시됨 (총 개수, 활성 개수)
   - [ ] 최근 랭킹 변동 알림이 표시됨
   - [ ] 최근 리뷰 요약이 표시됨
   - [ ] 활동 로그가 표시됨
   - [ ] Places 목록이 표시됨 (최근 순)

---

## 🐛 알려진 이슈 및 제한사항

### 스크래핑 관련
1. **네이버 차단 가능성**:
   - 짧은 시간에 많은 요청 시 IP 차단 가능
   - 해결: 스크래핑 간격을 5-10분 이상 유지

2. **Puppeteer 타임아웃**:
   - 네트워크가 느린 경우 타임아웃 발생 가능
   - 해결: `.env`의 `PUPPETEER_TIMEOUT` 값 증가 (기본 30000ms)

3. **DOM 구조 변경**:
   - 네이버가 페이지 구조를 변경하면 스크래핑 실패 가능
   - 해결: Firecrawl API 키 사용 (LLM 기반 추출, DOM 변경에 강건)

### Firecrawl API (선택적)
- 현재 Firecrawl API 키가 설정되지 않아 Puppeteer만 사용 중
- Firecrawl을 사용하려면:
  1. https://firecrawl.dev에서 API 키 발급
  2. `.env`에 `FIRECRAWL_API_KEY=your_api_key` 추가
  3. 서버 재시작

---

## 📊 성능 벤치마크 목표

### 스크래핑 성능
- 랭킹 스크래핑: 10-30초
- 리뷰 스크래핑: 15-45초
- 경쟁사 스냅샷: 10-20초

### API 응답 시간
- 인증 API: < 200ms
- Place/Keyword CRUD: < 100ms
- 히스토리 조회: < 500ms (데이터 양에 따라 증가)

---

## 💡 피드백 수집 항목

테스트하면서 다음 항목들을 체크해 주세요:

### 기능성
- [ ] 모든 핵심 기능이 예상대로 동작함
- [ ] 에러 처리가 적절함
- [ ] 로딩 상태가 명확하게 표시됨

### 사용성 (UX)
- [ ] UI가 직관적이고 이해하기 쉬움
- [ ] 네비게이션이 명확함
- [ ] 에러 메시지가 이해하기 쉬움

### 성능
- [ ] 페이지 로딩이 빠름
- [ ] 스크래핑 속도가 허용 가능함
- [ ] 차트 렌더링이 부드러움

### 버그 및 개선사항
- 발견한 버그나 개선사항을 기록해 주세요
- 백엔드 콘솔과 프론트엔드 브라우저 콘솔의 에러 로그 확인

---

## 🔧 트러블슈팅

### 서버가 시작되지 않는 경우
```bash
# 백엔드
npm run dev

# 프론트엔드
cd web && npm run dev
```

### 데이터베이스 연결 실패
1. PostgreSQL이 실행 중인지 확인
2. `.env` 파일의 데이터베이스 설정 확인
3. 마이그레이션 실행: `npm run migration:run`

### 프론트엔드 빌드 에러
```bash
cd web
rm -rf .next node_modules
npm install
npm run dev
```

---

## 📝 다음 단계

테스트 완료 후:
1. 발견한 이슈를 `FEEDBACK.md`에 기록
2. 개선사항 우선순위 정리
3. 추가 기능 요구사항 수집
4. 성능 최적화 계획 수립
